<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
         <title>{% block title %}Garagestore{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {{ importmap() }}
        
        {% endblock %}
    </head>
    <body>
        {% include("partials/header.html.twig") %}
        {% block body %}{% endblock %}
        
         <footer class="site-footer">  
            {% include("partials/footer.html.twig") %}
        </footer>
    </body>
</html>
