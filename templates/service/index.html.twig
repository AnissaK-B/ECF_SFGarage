{% extends 'base.html.twig' %}

{% block title %}Nos services{% endblock %}

{% block body %}
<main class="main">
    {% for service in services %}
        <h2 class="mt-30 mb-30">{{ service.title }}</h2>
        <div class="texte">
            <p>{{ service.text }}</p>
        </div>
    {% endfor %}
             

<div class="row">
    <div class ="col- d-flex">
        <div class="col-10 col-sm-8 col-lg-6 mt-50 mb-30">
                <img src="{{ asset('images/repar.jpg') }}" class="d-block mx-lg-auto img-fluid" alt="Bootstrap Themes" width="500" loading="lazy">
            </div>

        <div class="col-10 col-sm-8 col-lg-6 mt-50 mb-30">
                <img src="{{ asset('images/mecanique.jpg') }}" class="d-block mx-lg-auto img-fluid" alt="Bootstrap Themes" width="500" loading="lazy">
            </div>
   </div>
</div>
</main>

<div class="d-grid gap-2 d-md-flex justify-content-md-start mt-4">
            <a class="btn btn-primary btn-lg px-4 me-md-2" href="{{ path('app_contact') }}">Contactez-nous</a>
        </div>
{% endblock %}


